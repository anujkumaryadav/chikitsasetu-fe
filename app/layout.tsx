import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "ChikitsaSet - Your Trusted Healthcare Companion",
  description: "Comprehensive medical services, expert consultations, and personalized care solutions for a healthier tomorrow. Book appointments with qualified doctors and healthcare professionals.",
  keywords: "healthcare, medical services, doctor appointments, health consultations, medical care, ChikitsaSet",
  authors: [{ name: "ChikitsaSet Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${poppins.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
